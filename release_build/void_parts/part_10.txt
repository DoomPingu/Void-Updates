        elif auswahl == 3:
            return

def letzthafen_edda_truheninhalt():
    wiegenlied = letzthafen_status()[
        "wiegenlied"
    ]

    if not wiegenlied.get(
        "truhe_geoeffnet",
        False
    ):
        return

    ort_titel(
        "Eddas Haus - Inhalt der Truhe"
    )

    # -------------------------
    # BEREITS UNTERSUCHT
    # -------------------------

    if wiegenlied.get(
        "truhe_inhalt_untersucht",
        False
    ):
        text_ausgeben(
            "Die Truhe steht offen. Das alte Liedblatt und die übrigen "
            "Erinnerungsstücke liegen wieder ordentlich darin.",
            art="menue"
        )

        if item_besitzt(
            "lina_wiegenlied_abschrift"
        ):
            print()

            design_text(
                "Du trägst eine Abschrift von Linas Wiegenlied bei dir.",
                design="wissen",
                art="menue"
            )

        warte_auf_taste(
            "Zurück"
        )

        return

    # -------------------------
    # ERSTES ÖFFNEN
    # -------------------------

    text_ausgeben(
        "Du hebst den Deckel vorsichtig an."
    )

    print()

    text_ausgeben(
        "Obenauf liegt eine kleine Stoffpuppe. Das Tuch ist ausgeblichen, "
        "doch um ihren Hals ist noch ein schmales blaues Band geknotet."
    )

    print()

    text_ausgeben(
        "Darunter findest du ein mehrfach gefaltetes Blatt. "
        "Die Schrift darauf ist fein und gleichmäßig."
    )

    print()

    design_text(
        "Linas Handschrift",
        design="wissen",
        art="menue"
    )

    print()

    text_ausgeben(
        "Wenn die Laterne brennt,"
    )

    text_ausgeben(
        "zieht die Möwe heim."
    )

    text_ausgeben(
        "Wenn die Welle schweigt,"
    )

    text_ausgeben(
        "steigt die Sonne über den Stein."
    )

    print()

    text_ausgeben(
        "Unter den vier Zeilen steht, in derselben Handschrift:"
    )

    print()

    text_ausgeben(
        '"Für Mama, falls dir wieder eine Zeile fehlt. - Lina"'
    )

    seite_wechseln(
        "Eddas Haus - Inhalt der Truhe",
        "Edda Rehn"
    )

    text_ausgeben(
        "Edda ist hinter dich getreten. Lange sagt sie nichts."
    )

    print()

    text_ausgeben(
        "[Edda] Sie hat sich immer darüber lustig gemacht, "
        "dass ich die zweite und dritte Zeile vertauscht habe."
    )

    print()

    text_ausgeben(
        "[Edda] Danach habe ich das Lied jahrelang nur noch gesummt."
    )

    text_ausgeben(
        "[Edda] Irgendwann waren die Worte einfach weg."
    )

    print()

    text_ausgeben(
        "Edda nimmt das Blatt vorsichtig aus deinen Händen."
    )

    print()

    text_ausgeben(
        "[Edda] Nach Linas Tod habe ich alles hier hineingelegt."
    )

    text_ausgeben(
        "[Edda] Irgendwann wusste ich noch, dass die Truhe wichtig war. "
        "Nur nicht mehr, wie ich sie öffnen sollte."
    )

    print()

    text_ausgeben(
        "Sie liest die vier Zeilen noch einmal. Nicht singend. "
        "Nur leise genug, dass der Wind sie nicht übertönt."
    )

    print()

    text_ausgeben(
        "[Edda] Mach dir eine Abschrift."
    )

    text_ausgeben(
        "[Edda] Das Original bleibt bei mir."
    )

    print()

    text_ausgeben(
        "[Edda] Aber ein Lied, das nur in einer Truhe liegt, "
        "ist auch eine Art von Schweigen."
    )

    seite_wechseln(
        "Eddas Haus - Inhalt der Truhe"
    )

    # -------------------------
    # BELOHNUNGEN
    # -------------------------

    if not item_besitzt(
        "lina_wiegenlied_abschrift"
    ):
        item_hinzufuegen(
            "lina_wiegenlied_abschrift"
        )

    besonderen_fund_dokumentieren(
        "linas_wiegenlied",
        "abgeschrieben"
    )

    wiegenlied[
        "truhe_inhalt_untersucht"
    ] = True

    spieler[
        "quest_wiegenlied"
    ] = "abgeschlossen"

    quest_meldung(
        "abgeschlossen",
        "Das stumme Wiegenlied",
        belohnungen=[
            "Abschrift von Linas Wiegenlied"
        ]
    )

    erfolg_freischalten(
        "ein_lied_das_blieb"
    )

    warte_auf_taste(
        "Weiter"
    )

def letzthafen_edda_truhe():
    wiegenlied = letzthafen_status()[
        "wiegenlied"
    ]

    while True:
        ort_titel(
            "Eddas Haus - Alte Truhe"
        )

        if wiegenlied.get(
            "truhe_geoeffnet",
            False
        ):
            if not wiegenlied.get(
                "truhe_inhalt_untersucht",
                False
            ):
                text_ausgeben(
                    "Der Verschluss der kleinen Truhe steht offen. "
                    "Der Deckel lässt sich jetzt ohne Widerstand anheben.",
                    art="menue"
                )

                menue_trenner()

                print(
                    "1. Den Inhalt untersuchen"
                )

                print(
                    "2. Zurück"
                )

                auswahl = eingabe_menu(
                    1,
                    2,
                    sonder_tasten=()
                )

                if auswahl == 1:
                    letzthafen_edda_truheninhalt()

                return

            letzthafen_edda_truheninhalt()
            return

        text_ausgeben(
            "Die kleine Holztruhe ist dunkel vom Alter. "
            "An der Vorderseite sitzen vier drehbare Messingscheiben."
        )

        print()

        print("Laterne")
        print("Möwe")
        print("Welle")
        print("Sonne")

        print()

        text_ausgeben(
            "Die Scheiben lassen sich drehen. Vier Bilder, "
            "vier Positionen - offenbar in der Reihenfolge des Liedes."
        )

        wiegenlied[
            "truhe_untersucht"
        ] = True

        menue_trenner()

        print(
            "1. Die Messingscheiben einstellen"
        )

        print(
            "2. Bekannte Hinweise ansehen"
        )

        print(
            "3. Zurück"
        )

        auswahl = eingabe_menu(
            1,
            3,
            sonder_tasten=()
        )

        # -------------------------
        # RÄTSEL VERSUCHEN
        # -------------------------

        if auswahl == 1:
            symbole = [
                "Laterne",
                "Möwe",
                "Welle",
                "Sonne"
            ]

            reihenfolge = []
            abgebrochen = False

            for position in range(
                1,
                5
            ):
                ort_titel(
                    "Eddas Haus - Alte Truhe"
                )

                print(
                    f"Position {position} von 4"
                )

                if reihenfolge:
                    print()

                    print(
                        "Bisher: "
                        + " → ".join(
                            reihenfolge
                        )
                    )

                print()

                for nummer, symbol in enumerate(
                    symbole,
                    start=1
                ):
                    print(
                        f"{nummer}. {symbol}"
                    )

                abbrechen_nummer = (
                    len(symbole) + 1
                )

                print(
                    f"{abbrechen_nummer}. Abbrechen"
                )

                symbol_auswahl = eingabe_menu(
                    1,
                    abbrechen_nummer,
                    sonder_tasten=()
                )

                if (
                    symbol_auswahl
                    == abbrechen_nummer
                ):
                    abgebrochen = True
                    break

                symbol = symbole.pop(
                    symbol_auswahl - 1
                )

                reihenfolge.append(
                    symbol
                )

            if abgebrochen:
                continue

            ort_titel(
                "Eddas Haus - Alte Truhe"
            )

            design_text(
                " → ".join(
                    reihenfolge
                ),
                design="wissen",
                art="menue"
            )

            print()

            if reihenfolge == [
                "Laterne",
                "Möwe",
                "Welle",
                "Sonne"
            ]:
                text_ausgeben(
                    "Die letzte Scheibe rastet mit einem leisen "
                    "Klicken ein."
                )

                text_ausgeben(
                    "Im Inneren des Schlosses bewegt sich "
                    "ein alter Mechanismus."
                )

                print()

                text_ausgeben(
                    "Der Deckel hebt sich einen Fingerbreit."
                )

                wiegenlied[
                    "truhe_geoeffnet"
                ] = True

                spieler[
                    "quest_wiegenlied"
                ] = "truhe_geoeffnet"

                quest_meldung(
                    "update",
                    "Das stumme Wiegenlied",
                    "Untersuche den Inhalt der geöffneten Truhe."
                )

                warte_auf_taste(
                    "Weiter"
                )

                return

            text_ausgeben(
                "Du drehst die letzte Scheibe in Position. "
                "Für einen Moment geschieht nichts."
            )

            print()

            text_ausgeben(
                "Dann springt der Mechanismus zurück. "
                "Die Reihenfolge war nicht richtig."
            )

            warte_auf_taste(
                "Noch einmal überlegen"
            )

        # -------------------------
        # HINWEISE
        # -------------------------

        elif auswahl == 2:
            ort_titel(
                "Eddas Haus - Hinweise"
            )

            hinweise = []

            if wiegenlied.get(
                "laterne_hinweis",
                False
            ):
                hinweise.append(
                    "Edda erinnert sich: "
                    "Die Laterne steht am Anfang."
                )

            if wiegenlied.get(
                "sera_hinweis",
                False
            ):
                hinweise.append(
                    "Sera erinnert sich: "
                    "Auf die Laterne folgt die Möwe."
                )

            if wiegenlied.get(
                "namenstein_hinweis",
                False
            ):
                hinweise.append(
                    "Bei Lina Rehns Namen steht "
                    "die Möwe vor der Welle."
                )

            if wiegenlied.get(
                "register_hinweis",
                False
            ):
                hinweise.append(
                    "Das alte Hauszeichen der Rehns "
                    "zeigt Welle vor Sonne."
                )

            if not hinweise:
                print(
                    "Du hast noch keinen sicheren Hinweis "
                    "auf die Reihenfolge."
                )

            else:
                for hinweis in hinweise:
                    print(
                        f"- {hinweis}"
                    )

            warte_auf_taste(
                "Zurück"
            )

        elif auswahl == 3:
            return

def letzthafen_edda_haus():
    daten = letzthafen_status()

    wiegenlied = daten[
        "wiegenlied"
    ]

    erstmals = letzthafen_bereich_betreten(
        "edda_haus"
    )

    while True:
        ort_titel(
            "Eddas Haus"
        )

        if erstmals:
            text_ausgeben(
                "Das Haus mit der verwitterten blauen Tür steht "
                "etwas abseits der dichtesten Häuserreihe."
            )

            print()

            text_ausgeben(
                "Als du näher kommst, hörst du dieselbe wortlose "
                "Melodie, die der Wind bis zum Leuchtfeuer getragen hat."
            )

            print()

            text_ausgeben(
                "Nach deinem Klopfen öffnet eine ältere Frau. "
                "Hinter ihr ist der kleine Raum ordentlich, aber voll."
            )

            print()
            text_ausgeben(
                "[Edda] Du hast wegen des Summens hergefunden, oder?"
            )

            print()
            text_ausgeben(
                f'[{spieler["name"]}] Man hört es bis zum Leuchtfeuer.'
            )

            print()
            text_ausgeben(
                "[Edda] Dann trägt der Wind heute weiter als sonst."
            )

            wiegenlied[
                "edda_getroffen"
            ] = True

            erstmals = False

            warte_auf_taste(
                "Weiter"
            )

        else:
            text_ausgeben(
                "Hinter der verwitterten blauen Tür ist es still. "
                "Nur das Meer ist durch die Wände zu hören.",
                art="menue"
            )

        menue_trenner()

        print("1. Mit Edda reden")
        print("2. Die alte Truhe ansehen")
        print("3. Dich im Zimmer umsehen")
        print("4. Zurück zum Klippenweg")

        auswahl = eingabe_menu(
            1,
            4,
            sonder_tasten=()
        )

        if auswahl == 1:
            dialog_edda()

        elif auswahl == 2:
            letzthafen_edda_truhe()

        elif auswahl == 3:
            ort_titel(
                "Eddas Haus"
            )

            text_ausgeben(
                "Unter dem Fenster steht eine alte, leere Wiege. "
                "Darin liegen heute nur gefaltete Decken."
            )

            text_ausgeben(
                "An der Wand hängen getrocknete Kräuter, Garnrollen "
                "und zwei kleine Bilderrahmen, deren Glas blind geworden ist."
            )

            print()

            text_ausgeben(
                "Nichts im Raum wirkt verlassen. "
                "Alles hat seinen Platz, auch die Dinge, die längst "
                "nicht mehr gebraucht werden."
            )

            warte_auf_taste(
                "Zurück"
            )

        elif auswahl == 4:
            return


def letzthafen_altes_leuchtfeuer():
    erstmals = letzthafen_bereich_betreten(
        "altes_leuchtfeuer"
    )

    wiegenlied = letzthafen_status()[
        "wiegenlied"
    ]

    while True:
        ort_titel(
            "Altes Leuchtfeuer"
        )

        if erstmals:
            text_ausgeben(
                "Das alte Leuchtfeuer steht auf einem vorspringenden "
                "Teil der Klippe. Sein Mauerwerk ist vom Salz rau geworden."
            )

            print()

            text_ausgeben(
                "Heute genügt es als Orientierung für kleinere Boote. "
                "Unter dir schlägt das Meer gegen dunklen Fels."
            )

            erstmals = False

        else:
            text_ausgeben(
                "Wind streicht um das alte Mauerwerk des Leuchtfeuers.",
                art="menue"
            )

        hauptquest_fertig = (
            spieler.get(
                "quest_ein_name_fehlt"
            ) == "abgeschlossen"
        )

        if (
            hauptquest_fertig
            and not wiegenlied.get(
                "melodie_gehoert",
                False
            )
        ):
            print()

            text_ausgeben(
                "Zwischen zwei Windstößen hörst du etwas, das nicht "
                "vom Meer kommt."
            )

            print()

            text_ausgeben(
                "Jemand summt weiter oben am Klippenweg eine langsame "
                "Melodie. Keine Worte, nur wenige wiederkehrende Töne."
            )

            print()

            text_ausgeben(
                "Für einen Moment klingt sie wie ein Wiegenlied. "
                "Dann trägt der Wind sie wieder fort."
            )

            wiegenlied[
                "melodie_gehoert"
            ] = True

        menue_trenner()

        if (
            hauptquest_fertig
            and not wiegenlied.get(
                "edda_haus_bekannt",
                False
            )
        ):
            print("1. Der Melodie folgen")
            print("2. Über die Klippen sehen")
            print("3. Zurück zum Klippenweg")

            auswahl = eingabe_menu(
                1,
                3,
                sonder_tasten=()
            )

            if auswahl == 1:
                ort_titel(
                    "Klippenweg"
                )

                text_ausgeben(
                    "Du gehst langsam zurück zwischen die Häuser "
                    "und wartest immer wieder auf die nächsten Töne."
                )

                text_ausgeben(
                    "Schließlich führt dich die Melodie zu einem "
                    "schmalen Haus mit einer verwitterten blauen Tür."
                )

                wiegenlied[
                    "edda_haus_bekannt"
                ] = True

                if spieler.get(
                    "quest_wiegenlied",
                    "nicht_entdeckt"
                ) == "nicht_entdeckt":
                    spieler[
                        "quest_wiegenlied"
                    ] = "edda_suchen"

                    quest_meldung(
                        "neu",
                        "Das stumme Wiegenlied",
                        "Finde heraus, wer in dem Haus mit der "
                        "verwitterten blauen Tür die wortlose "
                        "Melodie summt."
                    )

                warte_auf_taste(
                    "Weiter"
                )

                return

            elif auswahl == 2:
                ort_titel(
                    "Altes Leuchtfeuer"
                )

                text_ausgeben(
                    "Weit unter dir ziehen schmale Boote durch "
                    "das graue Wasser."
                )

                text_ausgeben(
                    "Von hier oben wirkt Letzthafen kleiner, "
                    "als es zwischen den engen Gassen erscheint."
                )

                warte_auf_taste(
                    "Zurück"
                )

            elif auswahl == 3:
                return

        else:
            print("1. Über die Klippen sehen")
            print("2. Zurück zum Klippenweg")

            auswahl = eingabe_menu(
                1,
                2,
                sonder_tasten=()
            )

            if auswahl == 1:
                ort_titel(
                    "Altes Leuchtfeuer"
                )

                text_ausgeben(
                    "Weit unter dir ziehen schmale Boote durch "
                    "das graue Wasser."
                )

                text_ausgeben(
                    "Von hier oben wirkt Letzthafen kleiner, "
                    "als es zwischen den engen Gassen erscheint."
                )

                warte_auf_taste(
                    "Zurück"
                )

            elif auswahl == 2:
                return

def letzthafen_klippenweg():
    while True:
        erstmals = letzthafen_bereich_betreten(
            "klippenweg"
        )

        ort_titel(
            "Klippenweg"
        )

        if erstmals:
            text_ausgeben(
                "Der Klippenweg steigt oberhalb des Hafens zwischen "
                "schmalen Wohnhäusern an."
            )

            print()

            text_ausgeben(
                "Wäscheleinen spannen sich zwischen den Fassaden. "
                "Vor einigen Türen stehen Fischkisten, Werkzeug "
                "oder kleine Bänke."
            )

            print()

            text_ausgeben(
                "Weiter draußen wird die Bebauung dünner. "
                "Dort steht das alte Leuchtfeuer gegen den grauen Himmel."
            )

        else:
            text_ausgeben(
                "Wind zieht durch die engen Häuserreihen des "
                "Klippenwegs und trägt den Geruch des Meeres herauf.",
                art="menue"
            )

        daten = letzthafen_status()

        spuren = daten[
            "spuren"
        ]

        wiegenlied = daten[
            "wiegenlied"
        ]

        optionen = [
            (
                "Den Klippenweg entlanggehen",
                "entlanggehen"
            )
        ]

        if spuren.get(
            "marek_adresse_bekannt",
            False
        ):
            optionen.append(
                (
                    "Oberer Steg - Haus 14",
                    "haus_14"
                )
            )

        if wiegenlied.get(
            "edda_haus_bekannt",
            False
        ):
            if wiegenlied.get(
                "edda_getroffen",
                False
            ):
                edda_text = "Edda Rehns Haus"

            else:
                edda_text = (
                    "Haus mit der verwitterten blauen Tür"
                )

            optionen.append(
                (
                    edda_text,
                    "edda_haus"
                )
            )

        optionen.append(
            (
                "Zum alten Leuchtfeuer gehen",
                "leuchtfeuer"
            )
        )

        optionen.append(
            (
                "Zurück nach Letzthafen",
                "zurueck"
            )
        )

        menue_trenner()

        for nummer, (
            text,
            _aktion
        ) in enumerate(
            optionen,
            start=1
        ):
            print(
                f"{nummer}. {text}"
            )

        auswahl = eingabe_menu(
            1,
            len(optionen),
            sonder_tasten=()
        )

        aktion = optionen[
            auswahl - 1
        ][1]

        if aktion == "entlanggehen":
            ort_titel(
                "Klippenweg"
            )

            text_ausgeben(
                "Du gehst an dicht stehenden Wohnhäusern vorbei. "
                "Die meisten tragen nur eine Nummer oder gar keine "
                "sichtbare Kennzeichnung."
            )

            text_ausgeben(
                "Durch offene Türen hörst du Geschirr, Gespräche "
                "und das Schaben von Werkzeug über Holz."
            )

            print()

            text_ausgeben(
                "Letzthafen besteht nicht nur aus Hafen, Listen und "
                "alten Wahrzeichen. Hier wohnen einfach Menschen."
            )

            warte_auf_taste(
                "Zurück"
            )

        elif aktion == "haus_14":
            letzthafen_haus_14()

        elif aktion == "edda_haus":
            letzthafen_edda_haus()

        elif aktion == "leuchtfeuer":
            letzthafen_altes_leuchtfeuer()

        elif aktion == "zurueck":
            return

def letzthafen():
    while True:
        daten = letzthafen_status()