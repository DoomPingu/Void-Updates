    if spuren.get(
        "register_marek",
        False
    ):
        print()

        text_ausgeben(
            f'[{spieler["name"]}] Im Haus der Listen gibt es '
            "Einträge über deine Fahrten nach Eichenruh."
        )

        print()

        text_ausgeben(
            f'[{spieler["name"]}] '
            "Nicht nur einen. Über Jahre hinweg."
        )

        print()

        text_ausgeben(
            "Marek lässt den Blick auf seinen Händen ruhen."
        )

        print()

        text_ausgeben(
            "[Marek] Dann weiß ich wenigstens, "
            "dass es stimmt."
        )

        text_ausgeben(
            "[Marek] Auch wenn es sich nicht "
            "wie mein Leben anfühlt."
        )

    if besonderer_fund_status(
        "stationsregister"
    ) == "gelesen":
        print()

        text_ausgeben(
            f'[{spieler["name"]}] Dein Name steht außerdem im '
            "Register der alten Wegstation am Königsring."
        )

        print()

        text_ausgeben(
            "Marek schweigt einen Moment."
        )

        print()

        text_ausgeben(
            "[Marek] Dann war ich dort."
        )

        text_ausgeben(
            "[Marek] Ich glaube dir das."
        )

        print()

        text_ausgeben(
            "[Marek] Erinnern kann ich mich "
            "trotzdem nicht daran."
        )

    if (
        spuren.get(
            "sera_marek",
            False
        )
        or spuren.get(
            "register_marek",
            False
        )
        or besonderer_fund_status(
            "stationsregister"
        ) == "gelesen"
    ):
        seite_wechseln(
            "Gezeitensteg",
            "Marek Voss"
        )

    print()

    text_ausgeben(
        "Marek schaut über den Steg zu den Häusern oberhalb "
        "des Hafens."
    )

    print()

    if spuren.get(
        "register_marek",
        False
    ):
        text_ausgeben(
            "[Marek] Oberer Steg. Haus 14."
        )

        print()

        text_ausgeben(
            "[Marek] Wenn ich dort wirklich gewohnt habe, "
            "ist vielleicht etwas von meinem alten Leben geblieben."
        )

        print()

        text_ausgeben(
            "[Marek] Geh hin. Im Moment vertraue ich diesen "
            "Aufzeichnungen mehr als meinem eigenen Kopf."
        )

    else:
        text_ausgeben(
            "[Marek] Wenn ich wirklich von hier bin, "
            "muss irgendwo etwas über mich stehen."
        )

        text_ausgeben(
            "[Marek] Arbeit. Reisen. Eine Adresse. Irgendetwas."
        )

        print()

        text_ausgeben(
            "[Marek] Im Moment vertraue ich einem alten Register "
            "mehr als meinem eigenen Kopf."
        )

    spuren[
        "marek_getroffen"
    ] = True

    if spuren.get(
        "register_marek",
        False
    ):
        if spuren.get(
            "elian_getroffen",
            False
        ):
            spieler[
                "quest_ein_name_fehlt"
            ] = "marek_zu_elian_bringen"

            quest_meldung(
                "update",
                "Ein Name fehlt",
                "Bring Marek zu seinem Bruder Elian "
                "in der Bootswerkstatt."
            )

        elif spuren.get(
            "elian_hinweis",
            False
        ):
            spieler[
                "quest_ein_name_fehlt"
            ] = "elian_suchen"

            quest_meldung(
                "update",
                "Ein Name fehlt",
                "Finde Mareks Bruder Elian in seiner "
                "Bootswerkstatt am Gezeitensteg."
            )

        else:
            spieler[
                "quest_ein_name_fehlt"
            ] = "marek_wohnung_untersuchen"

            quest_meldung(
                "update",
                "Ein Name fehlt",
                "Suche Mareks frühere Unterkunft am "
                "Oberen Steg, Haus 14."
            )

    else:
        spieler[
            "quest_ein_name_fehlt"
        ] = "marek_identitaet_pruefen"

        quest_meldung(
            "update",
            "Ein Name fehlt",
            "Prüfe Letzthafens Aufzeichnungen "
            "auf Hinweise zu Marek Voss."
        )

    warte_auf_taste(
        "Weiter"
    )

def marek_abschlussgespraech():
    if spieler.get(
        "quest_ein_name_fehlt"
    ) != "marek_abschluss_gespraech":
        return

    spuren = letzthafen_status()[
        "spuren"
    ]

    ort_titel(
        "Gezeitensteg"
    )

    dialog_titel(
        "Marek Voss"
    )

    text_ausgeben(
        "Marek sitzt auf einer niedrigen Kiste nahe der Kaimauer. "
        "Auf seinen Knien liegt ein kleines, neues Notizbuch."
    )

    print()
    text_ausgeben(
        "[Marek] Also. Ich habe einen Namen."
    )

    text_ausgeben(
        "[Marek] Ich habe einen Bruder. Eine alte Adresse. "
        "Und offenbar mehr Jahre auf dem Königsring, als ich zählen kann."
    )

    print()
    text_ausgeben(
        f'[{spieler["name"]}] Hilft dir das?'
    )

    print()
    text_ausgeben(
        "[Marek] Es gibt mir keine Erinnerungen zurück."
    )

    text_ausgeben(
        "[Marek] Aber es gibt mir etwas, dem ich glauben kann, "
        "wenn mein eigener Kopf es nicht tut."
    )

    # -------------------------
    # OPTIONAL: RÜCKKEHRKERBEN
    # -------------------------

    if spuren.get(
        "rueckkehrkerben",
        False
    ):
        print()

        text_ausgeben(
            f'[{spieler["name"]}] Nela hat mir die Kerben an deinem '
            "alten Fenster gezeigt."
        )

        print()
        text_ausgeben(
            "[Marek] Nach jeder langen Fahrt eine."
        )

        print()

        text_ausgeben(
            "Marek lächelt kurz, ohne den Blick zu heben."
        )

        print()
        text_ausgeben(
            "[Marek] Klingt nach etwas, das ich getan hätte."
        )

    # -------------------------
    # OPTIONAL: WEGMÜNZEN
    # -------------------------

    if spuren.get(
        "wegmuenzen_gefunden",
        False
    ):
        print()

        text_ausgeben(
            f'[{spieler["name"]}] Unter einer Diele lagen außerdem '
            "drei alte Wegmünzen."
        )

        print()
        text_ausgeben(
            "[Marek] Eiche und Welle."
        )

        text_ausgeben(
            "[Marek] Eichenruh und Letzthafen."
        )

        print()
        text_ausgeben(
            "[Marek] Das dritte Zeichen kenne ich auch."
        )

        text_ausgeben(
            "[Marek] Ich kann dir nur nicht sagen, woher."
        )

    # -------------------------
    # MAREKS NEUES BUCH
    # -------------------------

    seite_wechseln(
        "Gezeitensteg",
        "Marek Voss"
    )

    text_ausgeben(
        "Marek schlägt das Notizbuch auf und schreibt langsam "
        "mehrere kurze Zeilen untereinander."
    )

    print()

    design_text(
        "Mein Name ist Marek Voss.",
        design="wissen",
        art="atmosphaere"
    )

    text_ausgeben(
        "Ich komme aus Letzthafen."
    )

    text_ausgeben(
        "Elian ist mein Bruder."
    )

    text_ausgeben(
        "Ich fahre den Königsring."
    )

    text_ausgeben(
        "Ich war in Eichenruh."
    )

    print()

    text_ausgeben(
        "Wenn ich das wieder nicht weiß:"
    )

    text_ausgeben(
        "Glaube diesem Buch."
    )

    spuren[
        "marek_notizbuch"
    ] = True

    # -------------------------
    # ROUTENMAPPE
    # -------------------------

    print()

    text_ausgeben(
        "Danach zieht Marek eine abgewetzte Ledermappe "
        "aus seiner Tasche."
    )

    print()
    text_ausgeben(
        "[Marek] Die hatte ich bei mir, als ich hier ankam."
    )

    text_ausgeben(
        "[Marek] Sera erkennt meine Handschrift. Elian auch."
    )

    print()
    text_ausgeben(
        "[Marek] Ich kann dir jede Steigung, jede brauchbare Raststelle "
        "und fast jeden alten Wegstein darin erklären."
    )

    text_ausgeben(
        "[Marek] Ich erinnere mich nur nicht daran, das alles "
        "aufgeschrieben zu haben."
    )

    print()
    text_ausgeben(
        "[Marek] Ich bleibe vorerst hier. Nimm sie."
    )

    text_ausgeben(
        "[Marek] Vielleicht bringt sie wenigstens jemanden "
        "zuverlässig ans Ziel."
    )

    seite_wechseln(
        "Gezeitensteg"
    )

    # -------------------------
    # BELOHNUNGEN
    # -------------------------

    if not item_besitzt(
        "mareks_routenmappe"
    ):
        item_hinzufuegen(
            "mareks_routenmappe"
        )

    routenkenntnis_lernen(
        "koenigsring"
    )

    spieler[
        "quest_ein_name_fehlt"
    ] = "abgeschlossen"

    quest_meldung(
        "abgeschlossen",
        "Ein Name fehlt",
        belohnungen=[
            "Mareks Routenmappe",
            "Routenkenntnis: Königsring"
        ]
    )

    text_ausgeben(
        "Mareks Erinnerungen sind nicht zurückgekehrt."
    )

    text_ausgeben(
        "Aber zum ersten Mal seit seiner Ankunft hat er etwas, "
        "woran er sich morgen festhalten kann."
    )

    warte_auf_taste(
        "Weiter"
    )

def dialog_marek():
    while True:
        ort_titel(
            "Gezeitensteg"
        )

        dialog_titel(
            "Marek Voss"
        )

        text_ausgeben(
            "Marek steht am Rand des Stegs und arbeitet "
            "an einem Stück Tauwerk."
        )

        print()

        print(
            "1. " + dialog_option(
                "marek",
                "erinnerung",
                "Woran erinnerst du dich?"
            )
        )

        print(
            "2. " + dialog_option(
                "marek",
                "faehigkeiten",
                "Das mit dem Tauwerk kannst du noch."
            )
        )

        print(
            "3. " + dialog_option(
                "marek",
                "karren",
                "Erinnerst du dich an deinen Karren?"
            )
        )

        print(
            "4. Gespräch beenden"
        )

        auswahl = eingabe_menu(
            1,
            4,
            sonder_tasten=()
        )

        print()

        # -------------------------
        # ERINNERUNGEN
        # -------------------------

        if auswahl == 1:
            dialog_thema_markieren(
                "marek",
                "erinnerung"
            )

            text_ausgeben(
                "[Marek] An heute. "
                "An einiges von gestern."
            )

            print()

            text_ausgeben(
                "[Marek] Meinen Namen kenne ich, "
                "weil andere Leute ihn mir genannt haben."
            )

            print()

            text_ausgeben(
                "[Marek] Aber wenn ich versuche, mich an den Mann "
                "zu erinnern, der diesen Namen sein ganzes Leben "
                "getragen haben soll ..."
            )

            print()

            text_ausgeben(
                "[Marek] Da ist nichts."
            )

            warte_auf_taste(
                "Weiter"
            )

        # -------------------------
        # FÄHIGKEITEN
        # -------------------------

        elif auswahl == 2:
            dialog_thema_markieren(
                "marek",
                "faehigkeiten"
            )

            text_ausgeben(
                "Marek betrachtet seine Hände."
            )

            print()

            text_ausgeben(
                "[Marek] Dabei muss ich nicht nachdenken."
            )

            text_ausgeben(
                "[Marek] Knoten. Gewicht. Räder. Pferde."
            )

            print()

            text_ausgeben(
                "[Marek] Meine Hände wissen, was sie tun."
            )

            text_ausgeben(
                "[Marek] Nur ich weiß nicht mehr, warum."
            )

            warte_auf_taste(
                "Weiter"
            )

        # -------------------------
        # KARREN
        # -------------------------

        elif auswahl == 3:
            dialog_thema_markieren(
                "marek",
                "karren"
            )

            text_ausgeben(
                "[Marek] Nein."
            )

            print()

            text_ausgeben(
                "[Marek] Das ist das Seltsame."
            )

            text_ausgeben(
                "[Marek] Du kannst mir beschreiben, "
                "was an einem Wagen nicht stimmt, und ich könnte "
                "dir wahrscheinlich sagen, wie man es repariert."
            )

            print()

            text_ausgeben(
                "[Marek] Aber meinen eigenen Karren sehe ich "
                "nicht vor mir."
            )

            text_ausgeben(
                "[Marek] Nicht einmal verschwommen."
            )

            warte_auf_taste(
                "Weiter"
            )

        elif auswahl == 4:
            return

def dialog_elian():
    while True:
        ort_titel(
            "Bootswerkstatt am Gezeitensteg"
        )

        dialog_titel(
            "Elian Voss"
        )

        spuren = letzthafen_status()[
            "spuren"
        ]

        text_ausgeben(
            "Elian arbeitet an einem halb fertigen Bootsrahmen."
        )

        print()

        print(
            "1. " + dialog_option(
                "elian",
                "marek_frueher",
                "Wie war Marek früher?"
            )
        )

        print(
            "2. " + dialog_option(
                "elian",
                "familie",
                "Erzähl mir etwas über eure Familie."
            )
        )

        print(
            "3. Gespräch beenden"
        )

        auswahl = eingabe_menu(
            1,
            3,
            sonder_tasten=()
        )

        print()

        if auswahl == 1:
            dialog_thema_markieren(
                "elian",
                "marek_frueher"
            )

            text_ausgeben(
                "[Elian] Zuverlässig. Stur. "
                "Und immer unterwegs."
            )

            print()

            text_ausgeben(
                "[Elian] Marek konnte drei Wochen fort sein "
                "und trotzdem am Abend seiner Rückkehr wissen, "
                "welcher Balken am Haus wieder locker war."
            )

            if spuren.get(
                "brueder_begegnung",
                False
            ):
                print()

                text_ausgeben(
                    "[Elian] Jetzt kennt er die Balken "
                    "vermutlich besser als mich."
                )

            warte_auf_taste(
                "Weiter"
            )

        elif auswahl == 2:
            dialog_thema_markieren(
                "elian",
                "familie"
            )

            text_ausgeben(
                "[Elian] Viel gibt es nicht zu erzählen."
            )

            print()

            text_ausgeben(
                "[Elian] Wir sind hier aufgewachsen. "
                "Marek wollte irgendwann mehr von der Straße sehen."
            )

            text_ausgeben(
                "[Elian] Ich bin bei den Booten geblieben."
            )

            print()

            text_ausgeben(
                "[Elian] Offenbar kann man weit weggehen "
                "und trotzdem näher an Zuhause sein als jetzt."
            )

            warte_auf_taste(
                "Weiter"
            )

        elif auswahl == 3:
            return


def letzthafen_elian_werkstatt():
    daten = letzthafen_status()

    spuren = daten[
        "spuren"
    ]

    erstmals = letzthafen_bereich_betreten(
        "elian_werkstatt"
    )

    ort_titel(
        "Bootswerkstatt am Gezeitensteg"
    )

    if erstmals:
        text_ausgeben(
            "Die Werkstatt mit den grünen Türen steht "
            "zwischen zwei niedrigen Lagerhäusern."
        )

        print()

        text_ausgeben(
            "Durch die offene Hälfte des Tores siehst du "
            "einen unfertigen Bootsrumpf auf schweren Böcken liegen."
        )

        print()

        text_ausgeben(
            "Ein breitschultriger Mann arbeitet mit einem "
            "Ziehmesser an einer Planke."
        )

    # -------------------------
    # ERSTES TREFFEN MIT ELIAN
    # -------------------------

    if not spuren.get(
        "elian_getroffen",
        False
    ):
        print()

        text_ausgeben(
            f'[{spieler["name"]}] Elian Voss?'
        )

        print()

        text_ausgeben(
            "Der Mann hält mitten in der Bewegung inne."
        )

        print()

        text_ausgeben(
            "[Elian] Ja. Was gibt es?"
        )

        print()

        if spuren.get(
            "marek_getroffen",
            False
        ):
            text_ausgeben(
                f'[{spieler["name"]}] Ich komme wegen deines Bruders. '
                "Ich habe Marek am Gezeitensteg gefunden."
            )

            print()

            text_ausgeben(
                "[Elian] Du hast ihn gefunden?"
            )

            print()

            text_ausgeben(
                "Du erzählst ihm, dass Marek am Gezeitensteg ist "
                "und sich an große Teile seines eigenen Lebens "
                "nicht erinnert."
            )

            print()

            text_ausgeben(
                "Elian legt das Werkzeug langsam "
                "auf die Werkbank."
            )

            print()

            text_ausgeben(
                "[Elian] Dann stimmt es also."
            )

            text_ausgeben(
                "[Elian] Ich hatte gehofft, "
                "die Leute übertreiben."
            )

        else:
            text_ausgeben(
                f'[{spieler["name"]}] Ich suche deinen Bruder Marek. '
                "Mehrere Aufzeichnungen haben mich zu dir geführt."
            )

            print()

            text_ausgeben(
                "[Elian] Marek ist wieder in Letzthafen?"
            )

            print()

            text_ausgeben(
                "Du erklärst ihm, was du bisher über "
                "den verschwundenen Fuhrmann herausgefunden hast."
            )

            print()

            text_ausgeben(
                "[Elian] Wenn du ihn findest, bring ihn her."
            )

            text_ausgeben(
                "[Elian] Und sag ihm nicht, er müsse sich erinnern."
            )

            text_ausgeben(
                "[Elian] Das haben vermutlich schon genug "
                "Leute versucht."
            )

        spuren[
            "elian_getroffen"
        ] = True

        if spuren.get(
            "marek_getroffen",
            False
        ):
            spieler[
                "quest_ein_name_fehlt"
            ] = "marek_zu_elian_bringen"

            quest_meldung(
                "update",
                "Ein Name fehlt",
                "Sprich mit Marek und bring ihn "
                "zu seinem Bruder Elian."
            )

        else:
            quest_meldung(
                "update",
                "Ein Name fehlt",
                "Finde Marek in Letzthafen und bring ihn "
                "zu seinem Bruder Elian."
            )

        warte_auf_taste(
            "Weiter"
        )

    # -------------------------
    # WERKSTATT-MENÜ
    # -------------------------

    while True:
        ort_titel(
            "Bootswerkstatt am Gezeitensteg"
        )

        text_ausgeben(
            "Harz, Sägespäne und feuchtes Holz bestimmen "
            "den Geruch der kleinen Werkstatt.",
            art="menue"
        )

        print()

        print(
            "1. Mit Elian reden"
        )

        print(
            "2. Die Werkstatt ansehen"
        )

        print(
            "3. Zurück zum Gezeitensteg"
        )

        auswahl = eingabe_menu(
            1,
            3,
            sonder_tasten=()
        )

        if auswahl == 1:
            dialog_elian()

        elif auswahl == 2:
            ort_titel(
                "Bootswerkstatt am Gezeitensteg"
            )

            text_ausgeben(
                "Werkzeuge hängen in sauberer Reihenfolge "
                "an der Wand."
            )

            text_ausgeben(
                "Neben einem halb fertigen Ruder stehen "
                "mehrere beschriftete Holzstücke."
            )

            print()

            text_ausgeben(
                "Nichts wirkt außergewöhnlich."
            )

            text_ausgeben(
                "Es ist eine Werkstatt, in der täglich "
                "gearbeitet wird."
            )

            warte_auf_taste(
                "Zurück"
            )

        elif auswahl == 3:
            return


def marek_elian_begegnung():
    spuren = letzthafen_status()[
        "spuren"
    ]

    if spuren.get(
        "brueder_begegnung",
        False
    ):
        return

    ort_titel(
        "Bootswerkstatt am Gezeitensteg"
    )
