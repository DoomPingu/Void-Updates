    eingabe_menu(
        1,
        1,
        sonder_tasten=()
    )

def hilda_karren_ereignis():
    if spieler.get(
        "quest_hilda"
    ) != "aktiv":
        return False

    ort_titel(
        "Wald - Verlassener Karren"
    )

    text_ausgeben(
        "Im feuchten Boden des Waldwegs ziehen sich "
        "tiefe Radspuren zwischen die Bäume."
    )

    text_ausgeben(
        "Sie verlassen den Weg in einem flachen Winkel, "
        "als hätte jemand den Wagen bewusst zur Seite gelenkt."
    )

    print()

    text_ausgeben(
        "Wenig später findest du einen Handelskarren."
    )

    text_ausgeben(
        "Er steht schief im weichen Boden, "
        "ist aber nicht umgestürzt."
    )

    print()

    text_ausgeben(
        "Mehrere Kisten mit Stoffen und Vorräten "
        "stehen noch immer auf der Ladefläche."
    )

    text_ausgeben(
        "Nichts wirkt geplündert."
    )

    menue_trenner()

    print("1. Den Karren genauer untersuchen")
    print("2. Nach Spuren des Händlers suchen")

    auswahl = eingabe_menu(
        1,
        2,
        sonder_tasten=()
    )

    ort_titel(
        "Wald - Verlassener Karren"
    )

    if auswahl == 1:
        text_ausgeben(
            "Du gehst langsam um den Karren herum."
        )

        print()

        text_ausgeben(
            "Die Deichsel ist unbeschädigt."
        )

        text_ausgeben(
            "Auch an den Rädern findest du keine Spur "
            "eines gewaltsamen Zusammenstoßes."
        )

        print()

        text_ausgeben(
            "Die Lederriemen für die Zugtiere hängen geöffnet "
            "und ordentlich gelöst an der Vorderseite."
        )

        text_ausgeben(
            "Jemand hat die Tiere ausgespannt."
        )

    elif auswahl == 2:
        text_ausgeben(
            "Du suchst den Boden rund um den Wagen ab."
        )

        print()

        text_ausgeben(
            "Keine Blutspuren."
        )

        text_ausgeben(
            "Keine Kampfspuren."
        )

        text_ausgeben(
            "Keine Schleifspuren, die auf einen "
            "fortgeschleppten Körper hindeuten."
        )

        print()

        text_ausgeben(
            "Ein einzelner Satz Fußspuren führt vom Karren weg, "
            "verliert sich aber bald auf dem festen Waldboden."
        )

    print()

    text_ausgeben(
        "Auf einer der Kisten erkennst du "
        "das Zeichen von Hildas Marktstand."
    )

    seite_wechseln(
        "Wald - Verlassener Karren"
    )

    text_ausgeben(
        "Unter dem Sitz des Kutschers steckt eine "
        "schmale Ledermappe."
    )

    print()

    text_ausgeben(
        "Darin liegt ein abgegriffenes Fahrtenbuch."
    )

    print()

    design_text(
        "Letzte Einträge",
        design="wissen",
        art="menue"
    )

    print()
    print(
        "12. des Monats"
    )

    print(
        "Eichenruh morgen vor Mittag. "
        "Hilda wartet auf die Stoffe."
    )

    print()
    print(
        "13. des Monats"
    )

    print(
        "Bin später losgekommen. "
        "Der Weg kommt mir heute länger vor."
    )

    print()
    print(
        "..."
    )

    print(
        "Habe dieselbe Wegmarkierung zweimal passiert."
    )

    print(
        "Das ergibt keinen Sinn."
    )

    print()
    print(
        "Die Klippen..."
    )

    print(
        "Ich muss zum Hafen."
    )

    print()
    print(
        "Wie hieß der Hafen?"
    )

    print()

    text_ausgeben(
        "Die letzten Worte sind deutlich unruhiger geschrieben "
        "als der Rest des Buches."
    )

    text_ausgeben(
        "Danach bleiben die Seiten leer."
    )

    seite_wechseln(
        "Wald - Verlassener Karren"
    )

    if not item_besitzt(
        "fahrtenbuch_haendler"
    ):
        item_hinzufuegen(
            "fahrtenbuch_haendler"
        )

    besonderen_fund_dokumentieren(
        "fahrtenbuch_haendler"
    )

    print()
    print("-" * 34)

    design_text(
        "BESONDERER FUND",
        design="wissen",
        art="ereignis"
    )

    print()
    print(
        "Fahrtenbuch des Händlers"
    )

    print("-" * 34)

    spieler[
        "quest_hilda"
    ] = "karren_gefunden"

    quest_meldung(
        "update",
        "Die verschwundene Lieferung",
        "Berichte Hilda vom verlassenen Karren."
    )

    warte_auf_taste(
        "Zurück zum Waldweg"
    )

    return True

def zufallsereignis(gold, heiltränke, diamanten, lebenspunkte, spiel_laeuft, besiegte_gegner, ort):
    if spiel_laeuft:
        ereignis = random.randint(1, 8)
        if ereignis == 1:
            print()
            text_ausgeben(
                "Ein umgestürzter Baum versperrt den Waldweg."
            )

            text_ausgeben(
                "Die Wurzeln haben einen großen Teil der "
                "feuchten Erde mitgerissen."
            )

            print()
            print("1. Über den Stamm klettern")
            print("2. Einen Weg darum herum suchen")

            hindernis_auswahl = eingabe_menu(
                1,
                2,
                sonder_tasten=()
            )

            if hindernis_auswahl == 1:
                print()
                text_ausgeben(
                    "Du ziehst dich am rauen Stamm hinauf "
                    "und kletterst vorsichtig auf die andere Seite."
                )

                text_ausgeben(
                    "Ein paar lose Äste brechen unter deinen "
                    "Stiefeln, doch du kommst sicher hinüber."
                )

            elif hindernis_auswahl == 2:
                print()
                text_ausgeben(
                    "Du verlässt den Weg für einige Minuten "
                    "und suchst zwischen den Bäumen nach einer "
                    "passierbaren Stelle."
                )

                text_ausgeben(
                    "Nach einem kleinen Umweg erreichst du "
                    "den Waldweg wieder."
                )
        elif ereignis == 2:
            print()
            text_ausgeben(
                "Zwischen Farnen hörst du leise Wasser über "
                "Steine laufen."
            )

            text_ausgeben(
                "Wenige Schritte neben dem Weg tritt eine "
                "kleine Quelle zwischen den Wurzeln hervor."
            )

            if lebenspunkte < 100:
                print()
                text_ausgeben(
                    "Du wäschst dir Gesicht und Hände und "
                    "ruhst dich einen Moment am kühlen Wasser aus."
                )

                heilung = min(
                    random.randint(5, 10),
                    100 - lebenspunkte
                )

                lebenspunkte += heilung

                print()
                design_text(
                    f"Du regenerierst {heilung} Lebenspunkte.",
                    design="positiv",
                    art="menue"
                )

                print(
                    f"Lebenspunkte: {lebenspunkte}/100"
                )

            else:
                print()
                text_ausgeben(
                    "Du trinkst etwas von dem kalten Wasser "
                    "und setzt deinen Weg erfrischt fort."
                )

        elif ereignis == 3:
            print()
            print("Ein Gegner taucht auf!")

            kampf_ergebnis = kampf_starten(
                lebenspunkte=lebenspunkte,
                heiltraenke=heiltränke,
                waffe=spieler["waffe"]
            )

            lebenspunkte = kampf_ergebnis["lebenspunkte"]
            heiltränke = kampf_ergebnis["heiltraenke"]

            if kampf_ergebnis["gewonnen"]:
                gefundenes_gold = random.randint(10, 30)
                gold += gefundenes_gold
                besiegte_gegner += 1

                print()
                print(f"Du findest {gefundenes_gold} Gold.")

                if besiegte_gegner == 4:
                    print()
                    print("Du spürst eine mächtige Präsenz im Wald...")

            elif kampf_ergebnis["besiegt"]:
                print()
                print("Game Over")
                spiel_laeuft = False

        elif ereignis == 4:
            print()
            text_ausgeben(
                "Etwas abseits des Weges entdeckst du "
                "eine alte Raststelle."
            )

            text_ausgeben(
                "Zwischen zwei Bäumen liegt ein Kreis aus "
                "verrußten Steinen."
            )

            text_ausgeben(
                "Die Asche darin ist längst kalt."
            )

            print()
            print("1. Die Raststelle untersuchen")
            print("2. Weitergehen")

            rastplatz_auswahl = eingabe_menu(
                1,
                2,
                sonder_tasten=()
            )

            if rastplatz_auswahl == 1:
                rastplatz_fund = random.randint(
                    1,
                    3
                )

                if rastplatz_fund == 1:
                    gefundene_menge = random.randint(
                        1,
                        2
                    )

                    print()
                    text_ausgeben(
                        "Unter einer halb verrotteten Plane "
                        "findest du etwas trockenes Brennholz."
                    )

                    item_hinzufuegen(
                        "holz",
                        gefundene_menge
                    )

                    print()

                    design_text(
                        f"{gefundene_menge}x Holz gefunden",
                        design="positiv",
                        art="menue"
                    )

                elif rastplatz_fund == 2:
                    gefundenes_gold = random.randint(
                        3,
                        8
                    )

                    print()
                    text_ausgeben(
                        "Zwischen Laub und alten Stoffresten "
                        "entdeckst du einen kleinen Lederbeutel."
                    )

                    gold += gefundenes_gold

                    print()

                    design_text(
                        f"{gefundenes_gold} Gold gefunden",
                        design="positiv",
                        art="menue"
                    )

                elif rastplatz_fund == 3:
                    print()
                    text_ausgeben(
                        "Du findest einige alte Fußspuren, "
                        "verkohlte Zweige und einen zerbrochenen "
                        "Holzlöffel."
                    )

                    text_ausgeben(
                        "Wer hier gerastet hat, ist offenbar "
                        "schon vor längerer Zeit weitergezogen."
                    )

            elif rastplatz_auswahl == 2:
                print()
                text_ausgeben(
                    "Du lässt die verlassene Raststelle "
                    "hinter dir und bleibst auf dem Weg."
                )

        elif ereignis == 5:



            # -------------------------
            # VERLETZTER REISENDER
            # -------------------------

            if (
                spieler["reisender_status"]
                == "nicht_getroffen"
            ):
                print()
                text_ausgeben(
                    "Am Rand des Weges sitzt ein Mann "
                    "mit dem Rücken gegen einen Baum."
                )

                text_ausgeben(
                    "Sein Mantel ist an mehreren Stellen "
                    "zerrissen und eine Hand liegt fest "
                    "auf seiner Seite."
                )

                print()
                text_ausgeben(
                    "Als er deine Schritte hört, "
                    "hebt er langsam den Kopf."
                )

                print()
                print(
                    "1. Nachsehen, ob du helfen kannst"
                )

                print(
                    "2. Abstand halten und weitergehen"
                )

                reisender_auswahl = eingabe_menu(
                    1,
                    2,
                    sonder_tasten=()
                )

                if reisender_auswahl == 1:
                    ort_titel(
                        "Wald - Verletzter Reisender"
                    )

                    text_ausgeben(
                        "Du gehst vorsichtig zu ihm "
                        "und kniest dich neben den Baum."
                    )

                    text_ausgeben(
                        "Die Verletzung sieht schmerzhaft aus, "
                        "aber nicht lebensbedrohlich."
                    )

                    text_ausgeben(
                        "Du hilfst ihm vorsichtig auf die Beine."
                    )

                    warte_auf_taste(
                        "Weiter"
                    )

                    dialog_titel("???")

                    text_ausgeben(
                        "[???] Danke..."
                    )

                    text_ausgeben(
                        "[???] Ich dachte schon, hier kommt "
                        "niemand mehr vorbei."
                    )

                    warte_auf_taste(
                        "Weiter"
                    )

                    text_ausgeben(
                        "Er blickt den Waldweg hinunter."
                    )

                    print()

                    text_ausgeben(
                        "[???] Seltsam."
                    )

                    text_ausgeben(
                        "[???] Ich war sicher, dass ich diesen "
                        "Abschnitt schon hinter mir hatte."
                    )

                    print()

                    text_ausgeben(
                        "Für einen Moment wirkt er, als wolle "
                        "er noch etwas sagen."
                    )

                    text_ausgeben(
                        "Dann schüttelt er nur den Kopf."
                    )

                    spieler[
                        "reisender_status"
                    ] = "geholfen"

                elif reisender_auswahl == 2:
                    print()
                    text_ausgeben(
                        "Du bleibst auf Abstand."
                    )

                    text_ausgeben(
                        "Der Mann beobachtet dich, sagt aber nichts."
                    )

                    print()
                    text_ausgeben(
                        "Nach einigen Schritten verschwindet "
                        "er hinter den Bäumen."
                    )

                    spieler["reisender_status"] = "ignoriert"

            # -------------------------
            # SPÄTERE SPUREN
            # -------------------------

            elif spieler["reisender_status"] == "geholfen":
                print()
                text_ausgeben(
                    "An einem niedrigen Ast hängt ein schmaler "
                    "Streifen dunklen Stoffes."
                )

                text_ausgeben(
                    "Das Material erinnert dich an den "
                    "zerrissenen Mantel des verletzten Reisenden."
                )

                print()
                text_ausgeben(
                    "Daneben führen einzelne Fußspuren "
                    "vom Waldweg fort."
                )

                text_ausgeben(
                    "Nach wenigen Metern verlierst du sie "
                    "zwischen Wurzeln und altem Laub."
                )

            elif spieler["reisender_status"] == "ignoriert":
                print()
                text_ausgeben(
                    "Du kommst an der Stelle vorbei, "
                    "an der du den verletzten Reisenden "
                    "zurückgelassen hast."
                )

                print()
                text_ausgeben(
                    "Der Platz unter dem Baum ist leer."
                )

                text_ausgeben(
                    "Nur einige niedergedrückte Blätter "
                    "zeigen noch, dass hier jemand gesessen hat."
                )             

        elif ereignis == 7:
            print()
            text_ausgeben(
                "Zwischen den Bäumen öffnet sich eine "
                "kleine Lichtung."
            )

            text_ausgeben(
                "Mehrere niedrige Steine ragen dort "
                "aus Moos und Farnen."
            )

            print()
            text_ausgeben(
                "Auf den ersten Blick wirken sie wie "
                "gewöhnliche Felsen."
            )

            text_ausgeben(
                "Als du näher trittst, erkennst du jedoch "
                "verwitterte Einkerbungen."
            )

            print()
            text_ausgeben(
                "Einer der Steine trägt noch den Rest "
                "eines eingemeißelten Pfeils."
            )

            text_ausgeben(
                "Er zeigt zwischen zwei dicht stehende "
                "Bäume."
            )

            print()
            text_ausgeben(
                "Dort ist heute kein Weg mehr zu erkennen."
            )


        elif ereignis == 8:
            print()
            text_ausgeben(
                "Ohne Vorwarnung erhebt sich ein Schwarm "
                "schwarzer Vögel aus den Baumkronen."
            )

            text_ausgeben(
                "Dutzende Flügel schlagen gleichzeitig "
                "durch die stille Luft."
            )

            print()
            text_ausgeben(
                "Für einige Sekunden verschwinden die "
                "Vögel über den Baumwipfeln."
            )

            print()
            text_ausgeben(
                "Dann wird es still."
            )

            print()
            text_ausgeben(
                "Nicht völlig still."
            )

            text_ausgeben(
                "Aber deutlich stiller als noch einen "
                "Moment zuvor."
            )

            print()
            text_ausgeben(
                "Du wartest kurz und hörst schließlich "
                "wieder Wind in den Blättern."
            )

            text_ausgeben(
                "Dann setzt du deinen Weg fort."
            )


        elif ereignis == 6:
            print()
            text_ausgeben(
                "Am Rand des Weges fallen dir frische Spuren "
                "im weichen Boden auf."
            )

            text_ausgeben(
                "Sie führen zwischen den Bäumen hindurch "
                "und verschwinden im Unterholz."
            )

            print()
            print("1. Die Spuren untersuchen")
            print("2. Auf dem Weg bleiben")

            spuren_auswahl = eingabe_menu(
                1,
                2,
                sonder_tasten=()
            )

            if spuren_auswahl == 1:
                spuren_fund = random.randint(
                    1,
                    3
                )

                if spuren_fund == 1:
                    print()
                    text_ausgeben(
                        "Du folgst den Abdrücken einige Minuten "
                        "zwischen den Bäumen."
                    )

                    text_ausgeben(
                        "Schließlich entdeckst du eine kleine "
                        "verlassene Feuerstelle."
                    )

                    text_ausgeben(
                        "Unter der Asche glimmt noch etwas Wärme."
                    )

                    print()
                    text_ausgeben(
                        "Wer hier gerastet hat, kann noch nicht "
                        "lange fort sein."
                    )

                elif spuren_fund == 2:
                    print()
                    text_ausgeben(
                        "Je weiter du den Spuren folgst, desto "
                        "deutlicher erkennst du gespaltene Hufabdrücke."
                    )

                    text_ausgeben(
                        "An einem Baum ist die Rinde in Kniehöhe "
                        "frisch aufgerissen."
                    )

                    print()
                    text_ausgeben(
                        "Ein Wildschwein muss hier erst vor "
                        "Kurzem entlanggekommen sein."
                    )

                    text_ausgeben(
                        "Du entscheidest dich, nicht noch weiter "
                        "in sein Revier vorzudringen."
                    )

                elif spuren_fund == 3:
                    print()
                    text_ausgeben(
                        "Zwischen den Abdrücken entdeckst du "
                        "schwarze Krümel auf dem Waldboden."
                    )

                    text_ausgeben(
                        "Als du einen davon zwischen den Fingern "
                        "zerreibst, erkennst du Holzkohle."
                    )

                    print()
                    text_ausgeben(
                        "Die Spur stammt vermutlich von jemandem, "
                        "der zwischen Wald und Köhlerei unterwegs war."
                    )

                    text_ausgeben(
                        "Nach einigen Metern verlierst du sie "
                        "zwischen trockenem Laub."
                    )

            elif spuren_auswahl == 2:
                print()
                text_ausgeben(
                    "Du lässt die Spuren hinter dir und bleibst "
                    "auf dem bekannten Waldweg."
                )
    
    return gold, heiltränke, diamanten, lebenspunkte, spiel_laeuft, besiegte_gegner, ort
    

if spieler.get("prolog_skip_startwaffe"):
    startwaffe_waehlen()

    warte_auf_taste("Weiter")

    spieler["prolog_skip_startwaffe"] = False

    spieler["ort"] = "Eichenruh"

    ort_nachricht_setzen(
        "Der Prolog wurde übersprungen.",
        "Du beginnst direkt mit Akt 1 in Eichenruh."
    )


while spieler["spiel_laeuft"]:
    rueckkehr_hinweise_pruefen()

    if spieler["ort"] == "Eichenruh":
        ort_titel("Eichenruh")

        ort_nachricht_anzeigen()

        if spieler.get("prolog_status") == "dorf_freigeschaltet":
            besuchte_orte = spieler.get(
                "prolog_besuchte_orte",
                {}
            )

            anzahl_besucht = sum(
                besuchte_orte.values()
            )

            print()

            design_text(
                f"Sieh dich in Eichenruh um. "
                f"({anzahl_besucht}/3 Orte besucht)",
                design="wissen",
                art="menue"
            )

        elif spieler.get("prolog_status") == "dorf_kennengelernt":
            print()

            design_text(
                "Aus Richtung der Schmiede hörst du "
                "Bjorn ungewöhnlich laut fluchen.",
                design="warnung",
                art="menue"
            )

        menue_trenner()

        print("1. Maras Gasthaus")
        print("2. Bjorns Schmiede")
        print("3. Marktplatz")
        print("4. Nach Hause")

        koehler_weg_frei = spieler.get(
            "prolog_status"
        ) in [
            "koehlerei_lieferung",
            "prolog_abgeschlossen"
        ]

        pfad_vergessen_frei = (
            weg_status(
                "pfad_des_vergessens"
            ) == "entdeckt"
        )

        if koehler_weg_frei:
            print("5. Weg zur Köhlerei")

        if pfad_vergessen_frei:
            print(
                "6. Pfad des Vergessens"
            )

        print()

        print("[S] Status")
        print("[I] Inventar")
        print("[J] Questlog/Erfolge")
        print("[Q] Speichern / Menü")

        if pfad_vergessen_frei:
            menu_auswahl = eingabe_menu(
                1,
                6,
                sonder_tasten=(
                    "s",
                    "i",
                    "j",
                    "q"
                )
            )

        elif koehler_weg_frei:
            menu_auswahl = eingabe_menu(
                1,
                5,
                sonder_tasten=(
                    "s",
                    "i",
                    "j",
                    "q"
                )
            )

        else:
            menu_auswahl = eingabe_menu(
                1,
                4,
                sonder_tasten=(
                    "s",
                    "i",
                    "j",
                    "q"
                )
            )

    elif spieler[
        "ort"
    ] == "Letzthafen":
        letzthafen()
        continue


    elif spieler["ort"] == "Wald":
        aktive_reise = spieler.get(
            "reise"
        )

        if not isinstance(
            aktive_reise,
            dict
        ):
            spieler["ort"] = "Eichenruh"

            ort_nachricht_setzen(
                "Du findest zurück auf einen vertrauten Weg.",
                "Wenig später erreichst du wieder Eichenruh."
            )

            continue

        ort_titel("Wald")

        ort_nachricht_anzeigen()

        if aktive_reise:
            design_text(
                f"Richtung: {aktive_reise['ziel']}",
                design="wissen",
                art="menue"
            )

            print(
                f"Fortschritt: "